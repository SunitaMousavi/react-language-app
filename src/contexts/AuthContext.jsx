import { createContext } from "react";

// This file only exports the context, no components
export const AuthContext = createContext();

// Creates an empty "communication channel" that your components will use to share authentication data.
